{
  "name": "matami-platform",
  "version": "2.0.0",
  "description": "Mat'ami — multi-tenant SaaS restaurant ordering & delivery platform",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run prisma:generate && npm run build -w web && npm run build -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "test": "npm run test -w server",
    "prisma:generate": "npm run prisma:generate -w server",
    "db:push": "npm run db:push -w server",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
